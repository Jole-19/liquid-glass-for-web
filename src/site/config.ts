/** Single source of truth for the links the site points at. */

export const REPO_OWNER = 'Jole-19';
export const REPO_NAME = 'liquid-glass-for-web';
export const REPO_URL = `https://github.com/${REPO_OWNER}/${REPO_NAME}`;

/** What a package manager accepts as an install target for a GitHub repo. */
export const REPO_SLUG = `github:${REPO_OWNER}/${REPO_NAME}`;

/** The name the package will have on npm. Also its import specifier today. */
export const PACKAGE_NAME = 'liquid-glass-web';
